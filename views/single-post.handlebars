<div class="post-container flex-col a-center j-center">
    <h1 class="post-title">{{title}}</h1>
    <p class="post-body">{{content}}</p>
    <div class="comment-container">
    {{#each comment}}
        {{content}}
    {{/each}}
    </div>
</div>